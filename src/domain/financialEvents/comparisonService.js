const sum = (items, key) => items.reduce((total, item) => total + BigInt(item.effects?.[key] || 0), 0n)
export const compareFinancialClassifications = ({ current = {}, events = [], version = 'financial-event-classifier-v1' }) => ['cash','consumption','invoice','result','netWorth'].map((metric) => { const currentMinor = BigInt(current[metric] || 0), shadowMinor = sum(events, metric), differenceMinor = shadowMinor - currentMinor; return { metric, currentMinor: currentMinor.toString(), shadowMinor: shadowMinor.toString(), differenceMinor: differenceMinor.toString(), severity: differenceMinor === 0n ? 'none' : 'warning', version, responsibleEventIds: differenceMinor === 0n ? [] : events.map((item) => item.id).filter(Boolean) } })
