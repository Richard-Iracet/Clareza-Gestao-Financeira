import{readFile}from'node:fs/promises';const schema=await readFile(new URL('../supabase/migrations/012_create_open_finance_gateway_schema.sql',import.meta.url),'utf8'),rls=await readFile(new URL('../supabase/migrations/013_open_finance_gateway_rls.sql',import.meta.url),'utf8'),checks={expandOnly:!/drop table|delete from|update public\.finance_states/i.test(schema),noSecrets:!/(client_secret|access_token|refresh_token)/i.test(schema),crossUserForeignKeys:(schema.match(/foreign key\(user_id/g)||[]).length>=8,rlsForced:/force row level security/.test(rls),anonBlocked:/revoke all.*anon/.test(rls),noDeleteGrant:!/grant delete/i.test(rls),officialSyncDisabled:/officialSync.*false/.test(schema)};console.log(JSON.stringify(checks,null,2));if(Object.values(checks).includes(false))process.exitCode=1
