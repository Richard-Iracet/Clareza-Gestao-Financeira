import { createClient } from '@supabase/supabase-js';import { compareFinancialClassifications } from '../src/domain/financialEvents/comparisonService.js';import { createFinancialEventRepository } from '../src/infrastructure/storage/financialEventRepository.js'
const arg=(name,fallback)=>{const index=process.argv.indexOf(name);return index>=0?process.argv[index+1]:fallback},userId=arg('--user',process.env.FINANCIAL_RULES_USER_ID);if(!userId)throw new Error('--user é obrigatório.');if(!process.env.SUPABASE_URL||!process.env.SUPABASE_SERVICE_ROLE_KEY)throw new Error('Credenciais Supabase são obrigatórias.')
const client=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false}}),repository=createFinancialEventRepository(client),events=await repository.listEvents({userId,from:arg('--from'),to:arg('--to'),limit:Number(arg('--limit','1000'))}),current=JSON.parse(process.env.FINANCIAL_CURRENT_TOTALS_JSON||'{}'),report=compareFinancialClassifications({current,events});console.log(JSON.stringify(report,null,2))
