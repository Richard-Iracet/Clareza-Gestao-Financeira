export const reconcileExternalAccounts=({previous=[],incoming=[],at=new Date().toISOString()})=>{const incomingById=new Map(incoming.map((item)=>[item.providerAccountId,item])),previousById=new Map(previous.map((item)=>[item.providerAccountId,item]));const accounts=[];for(const item of incoming){const old=previousById.get(item.providerAccountId);accounts.push(old?{...old,...Object.fromEntries(Object.entries(item).filter(([,value])=>value!==null&&value!==undefined)),deletedAt:null,tombstoneAt:null,reappearedAt:old.tombstoneAt?at:old.reappearedAt}:item)}for(const old of previous)if(!incomingById.has(old.providerAccountId))accounts.push({...old,status:'removed',deletedAt:old.deletedAt||at,tombstoneAt:old.tombstoneAt||at});return accounts}
